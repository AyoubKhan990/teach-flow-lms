PORT=5000
GOOGLE_API_KEY=YOUR_GOOGLE_GEMINI_API_KEY
IMAGE_PROVIDER=auto
IMAGE_API_KEY=YOUR_IMAGE_PROVIDER_API_KEY

# Text generation (optional)
# Provider order: "quality" (best models first) or "cost" (cheaper/faster first)
TEXT_PROVIDER_ORDER=quality

# Model overrides (defaults chosen for quality)
GOOGLE_TEXT_MODEL=gemini-1.5-pro-latest
OPENAI_TEXT_MODEL=gpt-4o
OPENROUTER_TEXT_MODEL=anthropic/claude-sonnet-4.5
